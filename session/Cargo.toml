[package]
name = "session"
version = "0.1.0"
authors = ["gndl"]

[dependencies]
failure = "0.1.5"
ringbuf = "0.2.1"
cpal = {git = "https://github.com/RustAudio/cpal"}
lv2_raw = "0.2"
lilv = { git = "https://github.com/grejppi/lilv-rs.git" }
talker = { path = "../talker" }
